## Todo



## Done

- Root-README und tools-README aktualisiert (Init-Fresh-Clone, Fetch-AI-Docs, Hinweis auf `-y`)
- `init-fresh-clone` Script sowie `fetch-ai-docs` Script im tools package erstellt und in `package.json` registriert
- Download-Ziele in `tmp/` festgelegt und explizit in `.gitignore` aufgenommen
- `init-fresh-clone` auf `commander` + `@inquirer/prompts` umgestellt und `-y/--yes` beibehalten
- `init-fresh-clone` im Root-`package.json` verlinkt
- Tools-README erweitert mit Beschreibung aller Tools-Skripte
- Root-README um kurze Script-Übersicht ergänzt
- `main-rules.md` um eine Sektion mit Learnings für einfache CLI-Skripte ergänzt
