{
  "name": "tools",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "lint": "eslint",
    "check-types": "tsc --noEmit",
    "fix": "eslint --fix .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  }
}