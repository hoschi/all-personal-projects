{
  "name": "@repo/db",
  "version": "0.0.0",
  "private": true,
  "exports": {
    "./*": "./src/*.ts"
  },
  "bin": {
    "copy": "./src/copy.ts",
    "copy-env": "./src/copy-env.ts"
  },
  "scripts": {
    "lint": "bunx eslint .",
    "check-types": "tsc --noEmit",
    "fix": "bunx eslint --fix . && bun run format",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "echo 'no tests'"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/node": "^22.15.3",
    "eslint": "^9.34.0",
    "typescript": "5.9.2"
  },
  "dependencies": {
    "@types/bun": "^1.3.8",
    "dotenv": "^16.4.5",
    "dotenv-cli": "^10.0.0",
    "postgres": "^3.4.4"
  }
}
