generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["video"]
}

model Video {
  id    Int    @id @default(autoincrement())
  titel String @unique

  @@schema("video")
}
