# SST

SST is a new TanStack Start application for speech-to-text workflows.

## Development

Run the app on port `3059`:

```bash
bun run dev --filter=@repo/sst
```

## Current Scope

- Base TanStack Start scaffold
- Tailwind + shadcn setup
- Initial editor shell with two text areas and a control strip
