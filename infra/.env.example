# Copy to infra/.env for local, machine-specific settings.
# The hostname is the FRITZ!Box device name that resolves in your LAN,
# e.g. if http://stefan:3012 works, use stefan.
FRITZBOX_DEVICE_HOSTNAME=
